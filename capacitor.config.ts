import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ana.myagenda',
  appName: 'myagenda',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
